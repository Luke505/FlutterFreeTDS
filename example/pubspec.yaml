name: freetds_example
description: Demonstrates how to use the freetds plugin.
publish_to: 'none'

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  freetds:
    path: ../
  tempo: ^0.5.2
  logger: ^2.0.2+1
  decimal: ^2.3.3
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_lints:

flutter:
  uses-material-design: true